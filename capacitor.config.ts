import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic web deploy to heroku',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
